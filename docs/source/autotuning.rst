Autotuning
==========

A module which implement an autotuning method to try several machine learning algorithms
over a set of hyperparameters and get the best combination of them.

.. automodule:: autotuning
    :members: